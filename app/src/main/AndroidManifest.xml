<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itpvt.iislam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Register_Signin_Activity" />
        <activity android:name=".Activities.ContentActivity" />
        <activity android:name=".Activities.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.Kalimas" />
        <activity android:name=".Activities.DetailsKalima" />
        <activity android:name=".Activities.Dua" />
        <activity android:name=".Activities.QuranDua" />
        <activity android:name=".Activities.Masnoon_dua_list" />
        <activity android:name=".Activities.MasnoonDuaDetails"></activity>
    </application>

</manifest>